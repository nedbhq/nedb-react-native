{
	"name": "nedb",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"start": "node node_modules/react-native/local-cli/cli.js start",
		"purge": "rm -rf node_modules/",
    "clean": "rm -rf $TMPDIR/react-* && watchman watch-del-all && npm cache clean && yarn cache clean",
		"postinstall": "react-native link react-native-fs",
		"test": "jest"
	},
	"dependencies": {
		"react": "~15.4.1",
		"react-native": "0.42.3",
		"nedb-react-native": "file:../"
	},
	"devDependencies": {
		"babel-jest": "19.0.0",
		"babel-preset-react-native": "1.9.1",
		"jest": "19.0.2",
		"react-test-renderer": "~15.4.1"
	},
	"jest": {
		"preset": "react-native"
	}
}